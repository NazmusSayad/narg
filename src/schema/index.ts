import { NoArgRoot } from '../NoArg/NoArgRoot'

/**
 * @deprecated Use `NoArg.type` instead
 * This will be removed in the next major version
 */
export default {
  string: NoArgRoot.string,
  number: NoArgRoot.number,
  boolean: NoArgRoot.boolean,
  array: NoArgRoot.array,
  tuple: NoArgRoot.tuple,
}
